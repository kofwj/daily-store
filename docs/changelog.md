# 每日更新

记给自己看：当天上了什么、店员要不要刷新。新的写在最上面。

格式：日期、一句话、店员是否有感。细节以 git 为准。

---

## 2026-08-30

- 芝麻周报：每周导入官方明细后，按周一到周日出可复制文案 / 图片 / Excel
- 兑付页合计改 SQL，不再拉全月明细；生产关掉模板热加载；品牌/政策开关/版本号按进程缓存
- 版本进 V 0.3.00，补丁号改两位（0.3.00 … 0.3.99）
- 安全加固：新口令至少 8 位；闲置 30 分钟掉线；备份恢复要再输口令并全员重登
- 店员已有口令不用改，还能登录。只有还停在默认 123456、或新建/自己改口令时才走新规则
- 店员刷新即可。若离开半小时，重新登录
- 重构部署 V 0.3.11：测试瘦身（conftest 统一登录 fixture、拆掉两个 `*_regression` 堆积文件、webhook 参数化，226→216 全绿）；代码收编样板（parse_int / parse_days / month_end）；修改审计五段 UNION 改表驱动；设置页 398 行 if/elif 拆成 action 分发表；清零调用死代码约 125 行
- 修 /report/delete 带非法店号会 500 的潜在 bug
- 备份脚本加固：代码 bundle 推送后自动只留最新 10 份（此前两台灾备机已堆到 26+ 份）；先传 .part 再改名，断线不留残缺文件；连备份机前清本机 ARP（手机隐私 MAC 重连会换地址，旧 ARP 表项会让包石沉大海）；灾备失败降级为警告，不再拦住部署收尾的版本提交
- 店员无感，不用刷新

## 2026-08-27

- 门店档案可停用 / 删除；生产缺真实目录时不再把示例店灌进库
- 同步时保留生产机真实门店目录，并挂进容器
- 政策编辑支持插图上传，正文桌面所见即所得（表格 / Markdown / 新旧对照绿红划法）；可恢复历史版本
- 新增填报偏差榜 + 政策已读情况（管理员）
- 部署门禁：提交前全量测试 + ruff + mypy + 锁依赖；代码灾备 bundle

## 2026-08-26

- 移动取数落表 + 审计；通报表 / 考核的笔算改用移动校准数（有取数的店按月覆盖）
- 登录页把「通泰零售运营中心」调得比「运营商日报」大
- 设置页巨石拆开，页面更快

## 2026-08-25

- 政策说明页上线：管理员写，店员 / 店长 / 区域经理须先点「已阅读」才能进其他页
- 局域网 Caddy 优先用 Cloudflare 真实 IP：店员公网登录记公网地址
- 通报表 / 评优改用移动校准数；单项第一按移动
- 服务器状态页（负载 / 内存 / 业务规模，仅管理员）
- 并发写入提升（workers 2→4）

## 2026-08-24

- 管理员默认口令改 6 位
- 通报表移动差额正负分色：红偏少、蓝偏多、绿持平；移>报蓝、移<报红
- 录移动取数只对照，不再改填报笔算

## 2026-08-23

- 通报表复制图片适配微信：生成预览提示长按转发
- 重置口令改为一键默认并弹窗确认

## 2026-08-19

- 改口令统一至少 6 位（店长 / 区域经理 / 管理员一样）
- 登录日志记下真实来源 IP，不再记 Caddy 容器地址；走 Cloudflare 记公网 IP
- 部署时强制重建 Caddy，避免 rsync 后仍读旧配置
- 洞察本周 vs 上周几张表布局调整（左对齐 / 居中 / 列宽）

## 2026-08-18

- 开票申请整条上线：考核按税务样表导出酬金 / 租赁开票申请（收成服务费 / 手续费，明细自动挂两行，可改可删）；可选结算月；8 月考核取 7 月酬金；开票改删记入修改审计
- 设置页居中布局，侧栏收成导航卡并理顺菜单名（我的账号 / 人员账号 / 门店档案 / 复盘模板）
- 洞察可只看有 / 无顾问门店；表格居中列宽收紧

店员：开票 / 设置 / 洞察样式变了。

## 2026-08-17

- 登录日志：管理员可看成功登录 / 退出（姓名、角色、IP），留 90 天
- 筛选条搜索框和下拉一样高
- 生产迁移加写锁，避免双 worker 同时启动撞版本
- 芝麻服务费可导入垫资：管理员垫资页「导入芝麻服务费」
- 页头 / 登录页 / `/health` 显示版本号（V 0.2.2）
- 修官方芝麻明细解析：标题合并行不再误判
- 导航精简：触客记录并进触客；审计/登录日志进设置
- 触客开口默认本店运营商顾问，可改
- 企业微信群机器人：保存日报/触客后自动发群（全局 + 按地市覆盖）
- 门店种子拆成示例 + local：真实店名人员不进 git
- 文档和脚本去掉内网地址、灾备域名；GitHub 只推当前快照
- 登录页标志 / 副标题 / 系统名可在设置里改
- 机外备份库没变化就跳过拷贝
- 设置里的修改审计、登录日志不再跳全屏，左侧导航还在
- 芝麻服务费导入后直接记已兑，不进兑付清单
- 笔算可填到 0.1；通报表可录移动取数（简称移，新增+高），带取数截止日；合计/格子/复盘对照系统同期

店员：垫资多一列「芝麻」。管理员：左上角能对版本。

## 2026-08-16

- 成交播报改称 **触客**；看板 / 记录显示成功率（成交 ÷ 触客）
- 通报表复盘：点名今日 AI / 笔算 / 直降有量的店，本月保留标杆和单项第一
- 成交、垫资、兑付、审计：全店视图可按 **地市、区域经理** 筛整批数据
- 门店下拉改为「地市 | 经理」分组切换（不再堆芯片）
- 生产机每小时备份到局域网盘 + 远端灾备
- 灾备预装站（只听本机 8099，不接 Cloudflare）
- Cloudflare Tunnel 切换清单：`docs/failover-cloudflare-tunnel.md`

店员：导航变成「触客播报 / 触客记录」。管理员：通报复制文案、全店筛选会变。

## 2026-08-15 前后（已在线上）

- 只读角色：店长看本店，区域经理看本区域；可看报表 / 通报 / 垫资，不能填
- 组织拆成账户表 + 门店档案
- 管理员成交记录默认今天全店；垫资默认本月全店
- 登录连错 5 次锁 15 分钟；默认口令必须改
- 垫资台账；已兑付锁定
- CSRF、会话 24 小时、真实 xlsx 导出
- 门店 SelectPanel 可搜索

---

## 以后怎么记

当天改完、同步到生产之后补三行即可：

```md
## YYYY-MM-DD

- 做了什么
- 店员要不要刷新 / 改口令 / 改收藏夹
```

不必把每次 commit 都抄进来。一次上线写一条。
