# 复制为 scripts/backup.env 后按需改（backup.env 已在 .gitignore 时可本地留存）
# 用法：set -a; . scripts/backup.env; set +a; ./scripts/backup_offsite.sh

PRIMARY_HOST=192.168.100.5
PRIMARY_USER=root
PRIMARY_SSH_PORT=22

# 局域网安卓 Termux
LAN_BACKUP_HOST=192.168.100.109
LAN_BACKUP_USER=jian
LAN_BACKUP_DIR=store-daily-backups
LAN_BACKUP_SSH_PORT=8022

# 公网 VPS
REMOTE_BACKUP=pve.anemy.org
REMOTE_BACKUP_USER=root
REMOTE_BACKUP_DIR=store-daily-backups
REMOTE_BACKUP_SSH_PORT=8022

KEEP_DAYS=30
